## 1. 定義色彩規範與全域樣式
- [x] 1.1 在 `src/index.css` 中定義 CSS 變數（Primary, Success, Warning, Danger, Backgrounds, Text colors）。
- [x] 1.2 統一全域背景色與文字色，確保高對比度。
- [x] 1.3 優化按鈕與輸入框的 hover/focus 狀態視覺效果。

## 2. 改進輸入模組 (Input Module)
- [x] 2.1 在 `InputModule.tsx` 中新增「已解析檔名預覽」區域。
- [x] 2.2 實作可折疊清單，顯示具體的檔名列表。
- [x] 2.3 調整輸入區與預覽區的佈局，確保在大檔案列表下依然好用。

## 3. 實作比對結果過濾 (Result Filtering)
- [x] 3.1 在 `ComparisonResultModule.tsx` 中新增過濾器狀態管理。
- [x] 3.2 修改 `ResultSummary.tsx`，讓各個狀態統計項目可點擊。
- [x] 3.3 實作過濾邏輯，根據選取的狀態篩選 `results` 陣列。
- [x] 3.4 在結果列表上方新增「過濾工具列」，包含：
    - 「顯示全部」按鈕。
    - 「顯示成功」按鈕。
    - 目前過濾狀態的提示。

## 4. 優化結果列表與項目的視覺效果
- [x] 4.1 更新 `ComparisonResultModule.css`，加強不同狀態項目的背景對比度。
- [x] 4.2 修正文字與背景重疊或顏色接近的問題（特別是錯誤訊息與實際路徑）。
- [x] 4.3 優化圖片預覽視窗的遮罩層與內容對比。

## 5. 整合與測試
- [x] 5.1 測試不同數據量下的過濾效能。
- [x] 5.2 驗證在深色/淺色模式切換下的顯示正確性（若支援）。
- [x] 5.3 邀請使用者確認配色是否符合預期。
