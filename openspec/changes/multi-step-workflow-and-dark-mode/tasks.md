## 1. 完善深色模式 (Dark Mode Enhancement)
- [x] 1.1 在 `index.css` 中優化深色模式變數，確保背景、卡片、邊框色調統一。
- [x] 1.2 加入捲軸 (Scrollbar) 的深色模式樣式。
- [x] 1.3 檢查所有文字顏色標籤在深色模式下的對比度。

## 2. 實作多步驟導覽元件 (Step Indicator)
- [x] 2.1 在 `src/App.tsx` 中新增步驟狀態管理 (`currentStep`)。
- [x] 2.2 建立步驟導覽列 UI，顯示「1. 輸入規格」、「2. 選擇目錄」、「3. 執行比對」。
- [x] 2.3 實作步驟切換的漸變效果。

## 3. 重構 App.tsx 佈局邏輯
- [x] 3.1 根據 `currentStep` 進行條件渲染。
- [x] 3.2 在每個模組底部新增導覽按鈕區（上一步/下一步）。
- [x] 3.3 實作步驟跳轉邏輯與資料校驗。

## 4. 調整模組元件 UI
- [x] 4.1 調整 `InputModule`：輸入完成後顯示下一步按鈕。
- [x] 4.2 調整 `FileSystemModule`：掃描完成後顯示下一步按鈕。
- [x] 4.3 調整 `ComparisonResultModule`：結果顯示頁面提供「重新開始」功能。

## 5. 測試與優化
- [x] 5.1 測試在不同步驟間切換時資料是否正確保留。
- [x] 5.2 驗證深色模式在系統切換時的即時反應。
- [x] 5.3 確保介面在多步驟導覽下更易於閱讀。
